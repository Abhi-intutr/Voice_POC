# frontend

React + Vite UI for the voice cloning POC. See the project root
[README.md](../README.md) for full setup/run instructions.

Quick start:

```bash
npm install
npm run dev
```

Talks to the Spring Boot backend at `http://localhost:8080` by default;
override with `VITE_API_BASE_URL` if needed.
